{
  "name": "esgi-node-mongodb",
  "version": "1.0.0",
  "description": "``` docker compose up ``` ``` docker compose exec mongo mongorestore -u root -p password ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^5.5.0"
  }
}
